define elem

freq 40
{
shift sine +2 elem -0 +8 -0
shift sine elem -0 -0 +16
}
# shift linear elem -0 -16 -0
# change aneme spec to better reflect usage
# axis-angle, better ordering
# <transform-type> <define> <amp> [function] [speed] <axis> [offset]
# examples
# shift elem +16 sine y
# shift elem +8 sine +2 y (from above)
# shift elem +2 sine +2 x +5
# shift elem +2 +2   x +5
# shift elem +2 sine +2 x
# shift elem +2 sine x
# shift elem +2 +2   x
# shift elem +2 x    +5
# shift elem +2 x
# OR we change syntax
# <transform-type> <define> <function>
# where function: <floating><func-name>(<floating><xyz><+-><floating>)
# function regex: "(?<amplitude>[+-]?\\d*\\.?\\d+(?:[eE][+-]?\\d+)?)?(?<type>(?:sin|cos))?\\((?<speed>[+-]?\\d*\\.?\\d+(?:[eE][+-]?\\d+)?)?(?<axis>[xyz])(?<offset>[+-]\\d*\\.?\\d+(?:[eE][+-]?\\d+)?)?\\)"
# shift elem 8sin(2x+5)
# shift elem 8sin(2x)
# shift elem 2(2x)
# shift elem 2(x)

# example: Silverfish
# shift head +0.5 sine x -0.125
# shift head 0.5sin(x-0.125)
